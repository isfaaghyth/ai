model:
  default: "z-ai/glm-5.2"
  execution: "qwen/qwen-3-coder-480b-a35b-instruct"
  provider: "openrouter"
  base_url: "https://openrouter.ai/api/v1"

terminal:
  backend: "local"
  cwd: "."
  timeout: 180

agent:
  max_turns: 90
  reasoning_effort: "high"

memory:
  memory_enabled: true
  user_profile_enabled: true

gateway:
  provider: "telegram"
